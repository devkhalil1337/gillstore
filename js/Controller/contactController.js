angular.module('myApp').controller("contactController", function ($scope,$rootScope, $q, $interval,navigationService,apiService) {

    function init() {
        console.log("I am init function of contactController");
    }
    init();
});